version: "3.9"
services:
  postgres:
    image: postgres:13.3
    environment:
      POSTGRES_DB: "server"
      POSTGRES_USER: "admin"
      POSTGRES_PASSWORD: "pgpwd4habr"
    ports:
      - "5432:5432"

  server:
    build:
      context: ./.
      dockerfile: ./Dockerfile
    ports:
      - "8020:5000"
    restart: always
